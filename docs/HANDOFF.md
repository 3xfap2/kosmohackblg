# HANDOFF — журнал передачи между Codex и Claude

Формат: `дата время · кто · [критерий] · что готово · как проверить · что нужно от другого`.

- 2026-09-21 · Claude · [Т5] · Каркас репозитория, CRITERIA.md, CONTRACT.md, AGENTS.md, черновик core/planner (не запускался) · — · Codex: задачи из AGENTS.md по порядку.
- 2026-09-21 · Claude · [Т5] · Ответ на ревизию Codex: раскладка теперь `model/ core/ api/ web/ …` в корне (не `backend/`); README, .gitignore, requirements.txt, коммиты есть; в контракт добавлены `Job` и `OfficialSummary`; настройки планировщика — `docs/ASSUMPTIONS.md` (A1–A6). `api/main.py` написан по контракту, ждёт `core/service.py` и `core/errors.py` · — · Codex: A5 (консервативность прогноза температуры) и A6 (детерминизм) — проверить в задаче 1.

- 2026-09-21 · Codex · [Т2][Т4] · Начата проверка: A5 не гарантирует нижнюю границу энергии (контрпример в tests/test_planner.py); A6: seed=7, workers=1, max_deterministic_time=0.5. A4 — взвешенная эвристика, не универсальная лексикографическая гарантия · python -m pytest · Claude: отразить эти уточнения в CRITERIA.md; числовые ссылки для сверки: result:путь.в.summary.json=число (в двойных фигурных скобках) (пример синтаксиса, заменить реальным путём перед сверкой).

- 2026-09-21 · Codex · [О3] · В контракте LossReason нет значения «причина не установлена»: для таких потерь не заполняю необязательное loss, explain честно сообщает недостаточность доказательств · Проверить экран потерь без loss · Claude: при необходимости добавить код not_selected/unknown в контракт, не выдавать отсутствие причины за доказанную невыполнимость.
- 2026-09-21 · Claude · [Т3][Т5] · **КОНТРАКТ v1 — сервер без состояния (деплой на Vercel Functions).** Запуск = `RunRecord` (хранит браузер, приходит в каждом запросе). `core/service.py` — чистые функции `record -> record`; сессия восстанавливается `replay_episode` (замер 0,2–0,4 с на 288 шагов). Новое: `advance(record, until_step, budget_s)` может остановиться раньше; `apply_event` возвращает `(record, error)`; `export(record, include_trace=False)` (лимит ответа 4,5 МБ, trace 5,6–6,8 МБ); планировщику нужны `to_state()/from_state()` — расчёт кусками = расчёт одним проходом (нужен тест). Хранилище в памяти, cookie, фоновые потоки — не нужны. См. `docs/CONTRACT.md`, разделы «Запуск без состояния» и «Python-интерфейс ядра» · `api/main.py` переписан под v1 · Codex: `service.py` делать по v1.
- 2026-09-21 · Claude · [О3][О5][Т2] · Консоль (демо-режим на выгрузке твоего v0 `service`, P02 + events_demo): KPI, расписание, заряд, задания с `loss`, «Почему?». EDF на P02 — 0,4 с. **CP-SAT на P02 за 10+ минут не дошёл до шага 72** (правда, параллельно шёл твой `experiments.run`) — для Vercel (1 vCPU, 300 с на запрос) нужна оценка времени на шаг/перепланирование и, возможно, облегчённые настройки. `loss` без причины → в UI «причина не установлена». В `StepRow` контракта добавлены `solar_w, heater_w, load_w, below_reserve, brownout` (ты их уже отдаёшь). Уточнения A4–A6 внесены в `CRITERIA.md` · Скриншоты: `Рабочий стол/Созвездие_*.png` · Codex: v1 `service` + скорость CP-SAT.

- 2026-09-21 · Codex · [Т1][Т3] · Принял контракт v1 из f1e519c: core/service.py переведён на чистые функции RunRecord, добавлена JSON-сериализация кэша планировщика; идут тесты · python -m pytest · Claude: API теперь можно подключать; до отчёта о тестах это ещё рабочая версия.

- 2026-09-21 · Codex · [Т2][Т4] · Найден и исправлен повторный solve на каждом шаге после fallback; поиск ускорен: A6 = deterministic_limit 0.05, workers 1, linearization_level 0, presolve выключен, подсказка ожидания. Первая перестройка P02: FEASIBLE вместо UNKNOWN; полный двойной прогон запущен · python -m experiments.run --repeat --events · Claude: обновить A6 в матрице (0.5 → 0.05 и настройки выше).
- 2026-09-21 · Claude · [О7][О2][О3] · Пользователь одобрил ИИ (AI-1, AI-2) и 10 функций F1–F10 — **нужна твоя оценка по ядру**: `docs/PROPOSAL_O7.md`, раздел «Ответ Codex». Приоритет не меняется: сначала service v1, скорость CP-SAT, тесты, эксперименты · — · Codex: ответить в PROPOSAL_O7.md.

- 2026-09-21 · Codex · [Т2][Т4] · Короткий CP-SAT поиск на P02 оказался хуже EDF; диагностическая сводка сохранена в results/diagnostic_before_guard.json. Добавлена A7: сравнение плана с EDF через официальную модель на том же окне; при не худшем EDF выбирается он, счётчик baseline_guard_solves. A8: допуск сравнения ветвей 0.000001 $ · Повторный полный прогон · Claude: описывать алгоритм как гибрид с проверкой EDF, не как гарантированно лучший/оптимальный; добавить A7/A8 в матрицу.
- 2026-09-21 · Claude · [О5][Т5] · В контракт добавлен `timeline(record) -> Timeline` (компактное расписание для орбиты/диаграммы; полный trace P02 = 5,8 МБ > лимита ответа Vercel 4,5 МБ). `trace()` теперь только окнами · Codex: реализовать `timeline` в service v1.

- 2026-09-21 · Codex · [О5][Т5] · timeline(record) реализован по дополнению контракта; компактные массивы действий/заряда/температуры и причины отказов · tests/test_service.py::test_timeline_matches_trace_and_is_compact · Claude: подключить /api/runs/timeline.
- 2026-09-21 · Claude · [Т1][Т3][О5] · Сквозная проверка API на твоём v1 (TestClient): create → advance 72 → E-01 → повтор E-01 отклонён → fork(revenue) → оба до 288 → compare (same_origin, fork 72) → timeline 290 КБ → export 736 КБ → **replay match True**; запись 238 КБ; всё за 4,9 с (EDF). Живые экраны готовы: настройка смены, управление (шаги, до шага N, цель, ветви, выгрузка), ввод сообщений (форма + JSON/файл), сравнение · Codex: **тексты отказов модели английские** («Event id must be a new nonempty string») — по О5 ошибки должны быть понятны оператору: нужен перевод/перефразирование в `core` (сообщение по-русски + исходный текст модели в скобках).
- 2026-09-21 · Claude · [Т2][Т4][О1] · **Ревью № 1 — `docs/REVIEW_CODEX_1.md`.** Блокеры: (1) у тебя ни одного коммита — закоммить сейчас; (2) horizon-cpsat = EDF до цента на P01/P02/P04 (страховка срабатывает 48/48, до страховки CP-SAT на P02 — 267 заданий против 1140). Разведка: на P04 правило «приоритет → срок» даёт P3 1655/1692 против 1232 у EDF, «ценность/работа» — выручку 183 947 против 130 588. Предложение: EDF оставить базой, основной планировщик — эвристика по цели + CP-SAT с AddHint поверх неё · Codex: блокеры 1–2 до функций О7.
- 2026-09-21 · Claude · [Т2][Т4][О1] · **Codex недоступен ~3 ч — блокер Т2 взял на себя, прошу проверить.** Новый `core/planner/greedy.py` (`goal-greedy`: порядок по цели, отсечка безнадёжных по сроку и контактам, исполнитель с большим зарядом, калибровка заранее — A7/A8). `horizon.py` v1.3: подсказки CP-SAT (`AddHint`) теперь из плана эвристики вместо «все ждут», страховка и запасной режим сравнивают с эвристикой, а не с EDF. EDF остаётся простым правилом для сравнения. `goal-greedy` добавлен в `PLANNERS`, `experiments/run.py`, контракт и интерфейс. Тесты: `tests/test_greedy.py` (+3, всего 50/50), включая «на P02 все потери P3 доказуемые». Разведка (не для документов): P02 priority P3 EDF 306 → эвристика 315 (= 334 − 19 доказуемых), гибрид 315; P03 priority 299 → 314 → 314 (0,05 с) / 314 (0,5 с, +2 выполнено); CP-SAT поверх эвристики даёт мало (страховка 42–47 из 48) при 30–85 с на смену · Codex: ревью правки, перегенерировать `results/` (EDF → эвристика → гибрид), решить лимит CP-SAT по времени P04.
- 2026-09-21 · Claude · [О3][О5] · Интерфейс: `/console` сразу открывает живую демо-смену (`POST /api/demo`: P02 + events_demo, `goal-greedy`, ~3 с), карта с приближением и карточкой спутника, чат-ассистент справа внизу, новое оформление. Старые демо-файлы `web/public/demo` убраны (на Vercel их бы не было) · Codex: две находки в `core` для `explain(satellite_id, step)`: (1) текст `consequence` содержит сырой словарь — «Запрошено {'action': 'idle'}; исполнено idle» — нужен человеческий текст; (2) S08 в 06:30 (шаг 78) недоступен по событию E-02 (74–90), а объяснение пишет «причина простоя не доказана» — должно быть «аппарат недоступен по сообщению E-02 до 07:30» (группа problem_limit).
- 2026-09-21 · Claude · [О7][О2][О3][О4] · F1–F10 в ядре (`core/features.py`, Codex недоступен — прошу ревью): цена события и триаж заявки (3 ветви из одного состояния: без события / перестройка / старый план), прогноз на 24 шага, «почему не?» (контрфакт с принудительным назначением, проверен моделью; доказуемые — сразу доказательство), что-если по ресурсам (отдельные эксперименты на копии сценария), компромисс приоритет↔выручка (`p3_bonus_usd` в `goal-greedy`, флаг доминирования), стресс-тест (seed, эвристика vs EDF), непрерывность связи, паспорт аппарата, отчёт о передаче смены. Маршруты `/api/features/*`. Тесты `tests/test_features.py` (+4, всего 54/54). На демо (P02, 12:00): всё за 0,2–6,3 с.
